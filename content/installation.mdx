`ash
# filename: example.sh
pip install affinity-code-viewer
`

- Python 3.8+
- pip

### Method 1: PyPI (Recommended)
```bash
pip install affinity-code-viewer
```

### Verifying installation
```bash
affinity --version
```
