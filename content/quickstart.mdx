`ash
# filename: example.sh
affinity --help
`


To get started quickly:

```bash
$ pip install affinity-code-viewer
Collecting affinity-code-viewer
  Downloading affinity_code_viewer-1.0.0-py3-none-any.whl (45 kB)
Installing collected packages: affinity-code-viewer
Successfully installed affinity-code-viewer-1.0.0

$ affinity --help
Usage: affinity [OPTIONS] [FILE]

  Affinity - A beautiful IDE-like terminal viewer.

Options:
  --watch         Live reload on file save
  --run           Execute the script and view output
  --diff FILE     Compare two files
  --tree          Show directory tree
  --focus LINES   Focus on specific lines (e.g., 10-20)
  --mirage        Enable Mirage mode (Beta)
  --help          Show this message and exit.
```
